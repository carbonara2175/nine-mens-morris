const assert=require('assert');const {MorrisGame,COORDS,MILLS,ADJ}=require('./script');
assert.equal(COORDS.length,24);assert.equal(MILLS.length,16);assert.equal(ADJ.length,24);ADJ.forEach((ns,i)=>ns.forEach(n=>assert(ADJ[n].includes(i))));
let g=new MorrisGame();[0,3,1,4,2].forEach(i=>assert(g.play(i)));assert(g.removing);assert.deepEqual(g.removable(),[3,4]);assert(!g.play(99));assert(g.play(3));assert.equal(g.count(2),1);assert.equal(g.turn,2);
g=new MorrisGame();g.stock=[0,0,0];g.board[0]=g.board[8]=g.board[10]=g.board[21]=1;g.board[3]=g.board[4]=g.board[5]=g.board[6]=2;g.turn=1;assert.deepEqual(g.targets(10),[9,11,18]);assert(g.play(10));assert(g.play(9));assert(g.removing);assert(g.play(6));assert.equal(g.turn,2);
g=new MorrisGame();g.stock=[0,0,0];g.board[0]=g.board[1]=g.board[2]=1;g.board[3]=g.board[4]=g.board[5]=2;g.turn=1;assert.equal(g.phase(),'FLY');assert(g.play(0));assert(g.targets(0).includes(23));assert(g.play(23));
g=new MorrisGame();g.stock=[0,0,0];g.board[0]=g.board[1]=g.board[2]=1;g.board[3]=g.board[4]=g.board[5]=2;g.turn=1;g.removing=true;g.stalemateTurns=4;assert(g.play(3));assert.equal(g.winner,1);assert.equal(g.winReason,'石が2個以下');assert.equal(g.stalemateTurns,0);
g=new MorrisGame();g.stock=[0,0,0];[1,9,14,22].forEach(i=>g.board[i]=1);[0,2,21,23].forEach(i=>g.board[i]=2);g.turn=1;g.finishTurn();assert.equal(g.winner,1);assert.equal(g.winReason,'合法手なし');
g=new MorrisGame();g.stock=[0,0,0];[0,3,6,9].forEach(i=>g.board[i]=1);[2,5,8].forEach(i=>g.board[i]=2);for(let i=0;i<9;i++)g.finishTurn();assert.equal(g.stalemateTurns,9);assert.equal(g.winner,0);g.finishTurn();assert.equal(g.winner,1);assert.equal(g.winReason,'10ターン膠着');
g=new MorrisGame();g.stock=[0,0,0];[0,3,6,9].forEach(i=>g.board[i]=1);[2,5,8].forEach(i=>g.board[i]=2);g.stalemateActive=true;g.stalemateTurns=6;g.finishTurn(true);assert.equal(g.stalemateTurns,0);g.reset();assert.equal(g.stalemateActive,false);assert.equal(g.stalemateTurns,0);assert.equal(g.winReason,'');
console.log('All game logic tests passed.');
